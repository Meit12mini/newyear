export const sum = (num1: number, num2: number):number => {
    return num1 + num2;
  // Реализация будет добавлена позже
  // Реализация будет добавлена позже
};

export const multiplay = (num1: number, num2: number):number => {
    return num1 * num2;
  // Реализация будет добавлена позже
};

export const isEven = (num1: number):boolean => {
    if (num1 % 2 === 0 && (num1*3)%2 === 0) {
        return true;
    } else {
        return false;
    }
  // Реализация будет добавлена позже
};

